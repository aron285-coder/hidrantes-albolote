# Dependencias y builds
node_modules/
dist/
dev-dist/
.wrangler/
*.tsbuildinfo

# Secretos: NUNCA en Git (04 §10). Solo .env.example, sin valores.
.env
.env.*
!.env.example
*.pem
*.key
*.asc

# Respaldos y datos personales (04 §9, §11.1)
*.sql.gpg
*.gpg
*.dump
*.tar
*.tar.gz
respaldos/
fotos/

# Artefactos generados de datos/ (los GeoJSON sí se committean, 04 §8)
*.pmtiles
# …salvo el mapa base publicado: el build no depende de Protomaps (DEC-062)
!public/mapabase/albolote.pmtiles
datos/*.html

# Supabase local
supabase/.temp/
supabase/.branches/

# Pruebas
playwright-report/
test-results/
blob-report/
coverage/

# Claude Code: la configuración local es personal
.claude/settings.local.json

# Sistema
.DS_Store
Thumbs.db
*.log
.dev.vars

# Worktree temporal de scripts/compatibilidad.ts (TR-107)
.anterior/
